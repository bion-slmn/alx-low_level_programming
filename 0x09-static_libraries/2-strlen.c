#include "main.h"

/**
 * _strlen - count the length of a string.
 * @s: is a pointer to the string
 *
 * Return: Always the lengthof string.
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
	}

	return (i);
}
